<div class="fixed inset-0 bg-black bg-opacity-40 flex items-center justify-center z-50">

  <div class="bg-white rounded-xl shadow-xl w-full max-w-3xl h-96 flex flex-col relative">

    <button class="absolute top-3 right-4 text-xl text-gray-500 hover:text-black">
      ✕
    </button>

    <div class="flex-1 overflow-y-auto p-6">
      <%= render "form", achievement: @achievement %>
    </div>

  </div>
</div>
