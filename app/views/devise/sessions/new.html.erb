<div class="flex flex-col items-center justify-center px-6 pt-8 mx-auto md:h-screen dark:bg-gray-900">

  <!-- Logo -->
  <%= link_to root_path,
      class: "flex items-center justify-center mb-8 text-2xl font-semibold lg:mb-10 dark:text-white" do %>
    <img src="https://vectorseek.com/wp-content/uploads/2023/08/Ielts-Logo-Vector.svg-.png" class="h-16"/>

  <% end %>

  <!-- Card -->
  <div class="w-full max-w-xl p-6 space-y-8 sm:p-8 bg-white rounded-lg shadow dark:bg-gray-800">

    <h2 class="text-2xl font-bold text-gray-900 dark:text-white">
      Sign in to platform
    </h2>

    <!-- Flash -->
    <% if flash[:alert] %>
      <div class="p-3 text-sm text-red-700 bg-red-100 rounded-lg">
        <%= flash[:alert] %>
      </div>
    <% end %>

    <!-- Devise Form -->
    <%= form_for(resource,
        as: resource_name,
        url: session_path(resource_name),
        html: { class: "mt-8 space-y-6" }) do |f| %>

      <!-- Email -->
      <div>
        <%= f.label :email,
            class: "block mb-2 text-sm font-medium text-gray-900 dark:text-white" %>

        <%= f.email_field :email,
            autofocus: true,
            autocomplete: "email",
            placeholder: "name@company.com",
            class: "bg-gray-50 border border-gray-300 text-gray-900 sm:text-sm rounded-lg
                    focus:ring-primary-500 focus:border-primary-500 block w-full p-2.5
                    dark:bg-gray-700 dark:border-gray-600 dark:text-white" %>
      </div>

      <!-- Password -->
      <div>
        <%= f.label :password,
            class: "block mb-2 text-sm font-medium text-gray-900 dark:text-white" %>

        <%= f.password_field :password,
            autocomplete: "current-password",
            placeholder: "••••••••",
            class: "bg-gray-50 border border-gray-300 text-gray-900 sm:text-sm rounded-lg
                    focus:ring-primary-500 focus:border-primary-500 block w-full p-2.5
                    dark:bg-gray-700 dark:border-gray-600 dark:text-white" %>
      </div>

      <!-- Remember + Forgot -->
      <div class="flex items-center justify-between">

        <% if devise_mapping.rememberable? %>
          <div class="flex items-center">
            <%= f.check_box :remember_me,
                class: "w-4 h-4 border-gray-300 rounded bg-gray-50
                        focus:ring-3 focus:ring-primary-300 dark:bg-gray-700" %>
            <%= f.label :remember_me,
                class: "ml-2 text-sm text-gray-900 dark:text-white" %>
          </div>
        <% end %>

        <%= link_to "Lost Password?",
            new_user_password_path,
            class: "text-sm text-primary-700 hover:underline dark:text-primary-500" %>
      </div>

      <!-- Submit -->
      <%= f.submit "Login to your account",
      class: "w-full px-5 py-3 text-base font-medium text-center text-white
              bg-red-600 rounded-lg hover:bg-blue-700
              focus:ring-4 focus:ring-blue-300" %>


      <!-- Register -->
      <div class="text-sm font-medium text-gray-500 dark:text-gray-400">
        Not registered?
        <%= link_to "Create account",
            new_user_registration_path,
            class: "text-primary-700 hover:underline dark:text-primary-500" %>
      </div>

    <% end %>
  </div>
</div>
